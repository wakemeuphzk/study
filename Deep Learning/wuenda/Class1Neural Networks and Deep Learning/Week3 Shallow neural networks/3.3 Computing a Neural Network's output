计算一个神经网络的输出

只有一个隐藏层的简单两层神经网络结构：
神经网络的符号惯例:x表示输入特征，a表示每个神经元的输出，W表示特征的权重，
上标表示神经网络的层数（隐藏层为1），下标表示该层的第几个神经元。

用圆圈表示神经网络的计算单元，逻辑回归的计算有两个步骤，首先你按步骤计算出Z，
然后在第二步中你以sigmoid函数为激活函数计算z（得出a），一个神经网络只是这样子做了好多次重复计算。
z=W^T*x+b
a=sigmoid(z)

向量化计算
