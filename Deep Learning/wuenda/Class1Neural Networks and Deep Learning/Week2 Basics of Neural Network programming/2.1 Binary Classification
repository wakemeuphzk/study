二分类
神经网络的基础知识

1.for循环————向量化

2.前向传播与反向传播

3.逻辑回归为例

图像的存储：
我们来看看一张图片在计算机中是如何表示的，为了保存一张图片，需要保存三个矩阵，它们分别对应图片中的红、绿、蓝三种颜色通道，
如果你的图片大小为64x64像素，那么你就有三个规模为64x64的矩阵，分别对应图片中红、绿、蓝三种像素的强度值。

python语言

注意：这里的每一个样本是一个列向量，而不是行向量，同样，最后的y是行向量


导数就是变化率
偏导就是多个变量的时候，针对某个变量的变化率
