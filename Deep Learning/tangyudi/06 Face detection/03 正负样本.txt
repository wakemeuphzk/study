推荐linux系统 ubuntu系统

对于正样本：先进行裁剪的操作（根据标注的坐标把人脸裁剪出来），可以借助opencv，来完成制作人脸数据。
         IoU值大于某个值的也当做正样本（即不一定是整个人脸，可以是人脸不全的数据），引入一些遮蔽等现象下的人脸
        人工检查一下，数据是否有问题。

opencv有很多图像的操作，有c++和python的两个接口

对于负样本：在原始的人脸样本图像中，随意的框选的结果（IoU值比较小的结果）

IoU(Intersection over Union):是一种测量在特定数据集中检测相应物体准确度的一个标准。
IoU相当于两个区域重叠的部分除以两个区域的集合部分得出的结果。
一般来说，这个score ＞ 0.5 就可以被认为一个不错的结果了。
